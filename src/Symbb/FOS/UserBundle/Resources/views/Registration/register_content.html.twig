{% trans_default_domain 'FOSUserBundle' %}
{% form_theme form getSymbbTemplate('forum') ~ '::form.html.twig' %}

<form action="" {{ form_enctype(form) }} method="post" role="form" class="form-horizontal">
    {{ form_row(form.email) }}
    {{ form_row(form.username) }}
    {{ form_row(form.plainPassword.first) }}
    {{ form_row(form.plainPassword.second) }}
    {{ form_rest(form) }}
    <div class="form-group">
        <label class="col-sm-2 control-label required"></label>
        <div class="col-sm-10">
            <div class="btn-group">
                <input type="submit" id="_submit" name="_submit" value="{{ 'registration.submit'|trans }}" class="btn btn-primary"/>
            </div>
        </div>
    </div>
</form>
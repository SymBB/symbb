{% if hasSymbbAccess('CoreUser', 'VIEW_RATING', object.getTopic().getForum()) %}
<div class="raiting">
    <div class="like_dislike">
        <button type="button" class="btn btn-default btn-sm {{ getDislikeCssClass(object) }}"{% if hasSymbbAccess('CoreUser', 'RATE', object.getTopic().getForum()) %} onclick="window.location.href='{{ path('_symbb_post_rating_like', {'name': object.getSeoName() , 'id': object.getId() , 'like': 'dislike'}) }}'{% endif %}">
            <div class="glyphicon glyphicon-thumbs-down"></div>
            {{ getSymbbDislikeCount(object) }}
        </button>
            <button type="button" class="btn btn-default btn-sm {{ getLikeCssClass(object) }}"{% if hasSymbbAccess('CoreUser', 'RATE', object.getTopic().getForum()) %} onclick="window.location.href='{{ path('_symbb_post_rating_like', {'name': object.getSeoName() , 'id': object.getId() , 'like': 'like'}) }}'{% endif %}">
            <div class="glyphicon glyphicon-thumbs-up"></div>
            {{ getSymbbLikeCount(object) }}
        </button>
    </div>
    <div class="clear"></div>
</div>
{% endif %}